<%= form_with(model: current_user.bookmarks.build) do |f| %>
  <div><%= hidden_field_tag :shop_id, @shop.id %></div>
  <%= f.button type: "submit", class: "bookmark_btn" do %>
    <i class="far fa-bookmark bm"></i>
  <% end %>
<% end %>
